# Проверка 0.3.1

- 21 тест прошёл: добавлены удаление дерева папок, восстановление исходных путей, офлайн-очередь с восстановлением до подключения, сохранность файлов других типов и новых внешних заметок, защита корня/корзины/устаревшего подтверждения, удаление пустых папок и повтор операций после сбоя.
- В браузере на изолированной коллекции OPFS: выбранная папка с четырьмя заметками исчезла из списка после подтверждения, все четыре записи появились в корзине с исходными путями и затем восстановились.

## Проверка 0.3.0

- 16 тестов файлового слоя прошли. Новые проверки: выборочное безвозвратное удаление, защита активных заметок и устаревшего выбора, очередь удаления без разрешения, защита внешних изменений, групповое восстановление с конфликтом имён.
- В браузере: новое меню и список справа, выбор всех записей, окно подтверждения с названиями, отмена удаления без изменений, восстановление двух тестовых заметок, пустая корзина и возврат к редактору. Само безвозвратное удаление проверено автоматическими тестами на изолированном файловом двойнике.

## Проверка 0.2.0

- 11 тестов проходят: дополнительно проверены ввод тегов, повторное открытие без разрешения, сохранение офлайн с последующей синхронизацией, сохранность обеих версий при конфликте, последовательность корзина/восстановление и повтор очереди после сбоя.
- В браузере на тестовой коллекции: сразу открывается последняя заметка; Markdown-заголовок отображается как заголовок; несколько тегов через пробел становятся отдельными метками без повторов; кнопки жирного текста и снятия форматирования меняют оформление; после перезагрузки содержимое и теги сохраняются.
- Редактор Tiptap собран локально в vendor/editor.js; сетевые CDN и удалённый код не используются.
- Проверена узкая панель 360 px и широкое окно. После визуального выделения файла чтение OPFS подтвердило Markdown с **выделением** и четырьмя сохранёнными тегами.

## Ранее проверено в 0.1.0

- `node --check app.js`, `node --check storage.js`, `node --check background.js`: синтаксис корректен.
- `node --test tests/storage.test.js`: 6 тестов прошли.
- В браузере через localhost и изолированную коллекцию OPFS: открытие коллекции, выбор заметки, изменение Markdown и тегов, статус успешной записи, перенос в корзину и восстановление в исходную подпапку.
- После навигации на тестовую страницу файлы перечитаны через File System Access API: новый текст и теги сохранились, путь восстановлен, корзина не содержит перемещённую заметку.
- Визуально проверен узкий интерфейс редактора.

Ещё требует проверки после загрузки расширения в Chrome: системный диалог выбора обычной папки, сохранение разрешения после перезапуска Chrome, нативная боковая панель и её положение в настройках Chrome. Браузерная проверка OPFS не заменяет эту проверку интеграции с расширением и обычной файловой системой.